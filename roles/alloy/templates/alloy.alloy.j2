// Enable authentication for HTTP
http {
/*
  auth {
    basic {
      password = "{{ alloy_http_server_password_b64 | b64decode }}"
      username = "{{ alloy_http_username }}"
    }
  }
*/
}

// Configure logging
logging {
  format = "json"
  level = "info"
}
